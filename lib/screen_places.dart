import 'package:flutter/material.dart';

class PlacesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Places')),
      body: const Center(child: Text('This is the Places page')),
    );
  }
}